export const lessonComputerScience = [
  {
    id: "l-data-structures",
    stage: "computer-science",
    module: "Data Structures",
    title: "Data Structures: Choosing the Right Shape",
    what: "A data structure is a way of organizing data so the operations a program needs can be performed efficiently and predictably. The important question is not which structure is 'best', but which operations matter: lookup, insertion, deletion, ordering, or traversal.",
    why: "Good software choices depend on constraints. Understanding the trade-offs between arrays, linked lists, stacks, queues, hash maps, trees, and graphs lets you choose deliberately instead of reaching for whatever structure you already know.",
    mentalModel: "Think of storage as different kinds of warehouses. An array is numbered shelving, a linked list is a chain of labeled boxes, a stack is a pile, a queue is a line, a hash map is an indexed cabinet, a tree is a hierarchy, and a graph is a network of relationships.",
    coreConcepts: ["abstract data types vs implementations", "arrays and dynamic arrays", "linked lists", "stacks and queues", "hash maps", "trees", "graphs", "operation trade-offs", "space vs time trade-offs"],
    example: "const stack = [];\nstack.push(\"page A\");\nstack.push(\"page B\");\nconsole.log(stack.pop()); // page B",
    code: "class Queue {\n  constructor() {\n    this.items = [];\n    this.head = 0;\n  }\n\n  enqueue(item) {\n    this.items.push(item);\n  }\n\n  dequeue() {\n    if (this.head >= this.items.length) return undefined;\n    const item = this.items[this.head];\n    this.head += 1;\n    return item;\n  }\n}\n\nconst jobs = new Queue();\njobs.enqueue(\"build\");\njobs.enqueue(\"test\");\nconsole.log(jobs.dequeue()); // build",
    commonMistakes: ["Choosing a structure because it is familiar without identifying the required operations.", "Assuming every array operation is constant time.", "Implementing a queue with repeated shift() calls without considering the cost for large inputs.", "Confusing a data structure's interface with one particular implementation."],
    practice: "For a browser history, printer queue, contact lookup, and family-tree model, choose a suitable data structure and explain the key operation that drove your choice.",
    challenge: "Implement a stack and a queue from scratch and write down the expected time complexity of each supported operation.",
    miniProject: "Build a small in-memory task scheduler using a queue, a priority representation, and a hash map for task lookup. Document the trade-offs.",
    resources: [{ title: "Open Data Structures", url: "https://opendatastructures.org/", note: "Free textbook covering practical data structures and their analysis." }],
    mastery: ["I can choose a data structure from required operations rather than habit.", "I can explain the trade-off behind that choice.", "I can implement basic stack, queue, and hash-map behavior without relying entirely on a library."]
  },
  {
    id: "l-big-o",
    stage: "computer-science",
    module: "Algorithms & Complexity",
    title: "Big O & Algorithmic Complexity",
    what: "Big O notation describes how an algorithm's resource usage grows as input size grows. It is a way to reason about scalability, not a stopwatch measurement for one run.",
    why: "An algorithm that feels instant with 100 records can become unusable with a million. Complexity analysis lets you spot those scaling problems before production traffic exposes them.",
    mentalModel: "Imagine counting people in a room. Checking every person is linear work; repeatedly cutting the search space in half is logarithmic work. Big O asks how the amount of work grows as the room gets larger.",
    coreConcepts: ["input size n", "O(1), O(log n), O(n), O(n log n), O(n²)", "best, average, and worst-case reasoning", "time vs space complexity", "dominant terms", "amortized cost", "why constants still matter in real systems"],
    example: "// O(n): every item may be inspected.\nfunction contains(items, target) {\n  for (const item of items) {\n    if (item === target) return true;\n  }\n  return false;\n}\n\n// A hash-map lookup is typically O(1) average case.",
    code: "function hasDuplicate(values) {\n  const seen = new Set();\n\n  for (const value of values) {\n    if (seen.has(value)) return true;\n    seen.add(value);\n  }\n\n  return false;\n}\n\n// Average: O(n) time, O(n) extra space.",
    commonMistakes: ["Treating Big O as an exact runtime prediction.", "Ignoring the size of the input when comparing algorithms.", "Calling nested loops automatically O(n²) when the loops may have independent bounds.", "Ignoring memory usage because only CPU time was considered."],
    practice: "Classify simple loops, nested loops, binary search, and hash-map lookup as constant, logarithmic, linear, or quadratic where appropriate.",
    challenge: "Take two solutions to the same search problem, estimate their time and space complexity, then explain which one scales better and why.",
    miniProject: "Create a benchmark that compares linear search, binary search on sorted data, and Set lookup across increasing input sizes. Record both complexity expectations and observed results.",
    resources: [{ title: "Big-O Cheat Sheet", url: "https://www.bigocheatsheet.com/", note: "Quick reference for common data-structure and algorithm complexities." }],
    mastery: ["I can estimate the dominant growth rate of a small algorithm.", "I can distinguish time complexity from measured execution time.", "I can discuss time-space trade-offs explicitly."]
  },
  {
    id: "l-search-sort",
    stage: "computer-science",
    module: "Algorithms & Complexity",
    title: "Searching, Sorting & Algorithm Trade-offs",
    what: "Searching and sorting are core algorithmic patterns. The right algorithm depends on whether data is ordered, how often it changes, how much memory is available, and what performance matters.",
    why: "These algorithms expose an important engineering lesson: a faster algorithm often requires assumptions or extra memory. Understanding those trade-offs transfers to almost every area of software.",
    mentalModel: "Searching a messy shelf means checking items; searching a sorted catalog lets you eliminate half the possibilities repeatedly. Sorting is the preparation cost that can make later searches cheaper.",
    coreConcepts: ["linear search", "binary search", "sorting as preprocessing", "insertion sort", "merge sort conceptually", "stable vs unstable sorting", "in-place vs extra-memory approaches", "comparator functions"],
    example: "function binarySearch(values, target) {\n  let low = 0;\n  let high = values.length - 1;\n\n  while (low <= high) {\n    const mid = Math.floor((low + high) / 2);\n    if (values[mid] === target) return mid;\n    if (values[mid] < target) low = mid + 1;\n    else high = mid - 1;\n  }\n\n  return -1;\n}",
    code: "const users = [\n  { name: \"Asha\", age: 31 },\n  { name: \"Dev\", age: 24 },\n  { name: \"Mira\", age: 28 }\n];\n\nconst byAge = [...users].sort((a, b) => a.age - b.age);\nconsole.log(byAge);\n// Copy first when preserving the original array matters.",
    commonMistakes: ["Using binary search on data that is not sorted according to the same ordering.", "Mutating an array with sort() when other code still expects the original order.", "Comparing algorithms without stating their assumptions.", "Using a sophisticated algorithm when the input is tiny and simplicity is more valuable."],
    practice: "Implement linear search and binary search, then write the precondition that must be true before binary search is valid.",
    challenge: "Implement insertion sort from scratch and compare its behavior on nearly sorted data versus randomly ordered data.",
    miniProject: "Build a searchable contact index. Compare unsorted linear lookup against a sorted binary-search approach and a Map-based approach. Explain when each wins.",
    resources: [{ title: "VisuAlgo", url: "https://visualgo.net/en", note: "Interactive visualizations for searching, sorting, and other algorithms." }],
    mastery: ["I can explain the preconditions of binary search.", "I can compare algorithms by both complexity and practical assumptions.", "I can recognize when preprocessing or extra memory is worthwhile."]
  },
  {
    id: "l-memory-model",
    stage: "computer-science",
    module: "Memory & Execution",
    title: "Memory: Stack, Heap & References",
    what: "Programs need memory for active execution and for data that must remain available. At a high level, the call stack tracks active function calls while dynamically allocated objects live in managed memory often described as the heap.",
    why: "Understanding references and memory lifetime explains bugs involving mutation, aliasing, recursion depth, retained objects, and memory leaks.",
    mentalModel: "The call stack is a stack of active work. The heap is a larger storage area where dynamically created objects can live. A variable can hold a reference to an object rather than containing the object itself.",
    coreConcepts: ["call stack frames", "heap-allocated objects", "references and identity", "value vs reference semantics in JavaScript", "garbage collection", "retained references", "stack overflow", "memory leaks"],
    example: "const a = { count: 1 };\nconst b = a;\nb.count = 2;\nconsole.log(a.count); // 2\n\n// a and b reference the same object.",
    code: "function recurse(n) {\n  if (n === 0) return;\n  recurse(n - 1);\n}\n\nrecurse(100);\n// Very deep recursion can exceed the call stack.",
    commonMistakes: ["Saying JavaScript variables simply 'contain objects' without considering references.", "Assuming garbage collection means memory can never leak.", "Copying an object with a shallow spread and expecting deeply nested objects to be independent.", "Ignoring unbounded recursion or unbounded in-memory collections."],
    practice: "Draw the references for const a = {x: 1}; const b = a; const c = {...a}; and explain which mutations affect which objects.",
    challenge: "Find an example of a closure or event listener retaining an object longer than expected and explain how the reference keeps it reachable.",
    miniProject: "Create a small memory-behavior notebook containing examples of aliasing, shallow copies, deep copies, recursion depth, and object lifetime. Write the expected result before executing each example.",
    resources: [{ title: "MDN — Memory management", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Memory_management", note: "JavaScript-specific overview of allocation, garbage collection, and memory behavior." }],
    mastery: ["I can distinguish a value from a reference to an object.", "I can explain why unreachable objects can be collected.", "I can identify simple causes of stack overflow and retained-memory problems."]
  },
  {
    id: "l-processes-threads",
    stage: "computer-science",
    module: "Operating Systems & Concurrency",
    title: "Processes, Threads & Concurrency",
    what: "A process is a running program with its own operating-system-managed resources; threads are execution paths within a process that can share process memory. Concurrency is about managing overlapping work, while parallelism means work actually executing at the same time on multiple execution units.",
    why: "Modern software handles many tasks simultaneously: servers serve multiple clients, operating systems run many applications, and CPU-intensive work may use multiple cores. Confusing concurrency with parallelism leads to incorrect performance assumptions.",
    mentalModel: "A process is a workshop with its own equipment. Threads are workers inside that workshop who can share the same workspace. Multiple workshops are isolated from one another more strongly than workers inside one workshop.",
    coreConcepts: ["processes", "threads", "shared memory", "context switching", "concurrency vs parallelism", "race conditions", "locks and synchronization", "CPU-bound vs I/O-bound work", "worker threads/processes at a high level"],
    example: "// JavaScript in a browser commonly runs application code on one main thread,\n// while the runtime provides asynchronous facilities and may use other\n// threads/processes internally. Concurrency does not mean two JS statements\n// execute at the exact same instant on the same thread.",
    code: "// A race-like logical bug can happen when shared state is\n// read, modified, and written by overlapping operations.\nlet balance = 100;\n\nfunction spend(amount) {\n  if (balance >= amount) balance -= amount;\n}\n\n// In truly concurrent shared-memory systems, the check/update\n// may require synchronization to make the operation atomic.",
    commonMistakes: ["Using concurrency and parallelism as synonyms.", "Assuming every asynchronous operation creates a new JavaScript thread.", "Ignoring shared-state races when multiple execution contexts can modify the same resource.", "Using parallelism for an I/O-bound task without measuring whether it helps."],
    practice: "Classify ten scenarios such as downloading files, rendering a video, handling web requests, and calculating hashes as mainly I/O-bound or CPU-bound.",
    challenge: "Design a producer-consumer system on paper and identify where a shared queue needs synchronization in a multithreaded implementation.",
    miniProject: "Build a small experiment comparing sequential work with concurrent asynchronous I/O in Node.js. Explain why overlapping waiting time can improve throughput even without CPU parallelism.",
    resources: [{ title: "MDN — JavaScript execution model", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Execution_model", note: "Useful foundation for understanding JavaScript's execution model and concurrency behavior." }],
    mastery: ["I can distinguish process, thread, concurrency, and parallelism.", "I can identify when shared state creates a race risk.", "I can explain why asynchronous I/O is not the same thing as running JavaScript on another thread."]
  },
  {
    id: "l-networking-deeper",
    stage: "computer-science",
    module: "Computer Networking",
    title: "Networking Internals: TCP/IP, Ports & TLS",
    what: "Network communication moves data through layered protocols. IP provides addressing and routing, TCP provides a reliable ordered byte stream, ports identify application endpoints, and TLS adds encryption and authentication to a secure connection such as HTTPS.",
    why: "Foundation taught the conceptual Internet, DNS, HTTP, and request/response. This layer explains what happens underneath those concepts and gives you the vocabulary to debug real network problems.",
    mentalModel: "Think of a postal system with layers: IP gets a packet toward the right machine, a port gets it to the right service, TCP manages a reliable conversation, and TLS creates a protected channel before application data such as HTTP travels through it.",
    coreConcepts: ["IP addressing", "TCP vs UDP", "ports", "connections and sockets", "three-way handshake concept", "packet loss and retransmission", "TLS encryption and certificate authentication", "HTTPS as HTTP over TLS", "latency vs bandwidth"],
    example: "Browser → DNS resolution → TCP connection to server:443 → TLS handshake → HTTP request → HTTP response.\n\nThe exact sequence can vary with protocol versions, connection reuse, caching, and HTTP/3.",
    code: "// Node.js example: inspect a URL without implementing TCP yourself.\nconst url = new URL(\"https://example.com:443/products?id=42\");\n\nconsole.log(url.hostname); // example.com\nconsole.log(url.port || \"443 by default for https\");\nconsole.log(url.pathname); // /products",
    commonMistakes: ["Thinking DNS is the same thing as HTTP.", "Assuming TCP guarantees low latency; it provides reliability, not instant delivery.", "Treating a port as a physical network location rather than a logical service endpoint.", "Assuming HTTPS means the server itself is trustworthy without considering certificate validation and the broader trust model."],
    practice: "Trace an HTTPS request from a browser to a server and label where DNS, IP, TCP, TLS, and HTTP participate.",
    challenge: "Use browser DevTools to inspect a request and identify its protocol, remote endpoint, status, timing, and response size. Explain which measurements relate to latency versus payload transfer.",
    miniProject: "Create a network request anatomy document for one real website: DNS lookup, connection, TLS, HTTP request/response, caching, and approximate timing. Use DevTools evidence rather than guessing.",
    resources: [{ title: "Cloudflare Learning Center — TCP/IP", url: "https://www.cloudflare.com/learning/ddos/glossary/tcp-ip/", note: "Accessible explanations of core networking concepts." }],
    mastery: ["I can explain the roles of IP, TCP, ports, TLS, and HTTP separately.", "I can read basic network timing information in DevTools.", "I can reason about latency, reliability, and bandwidth as different concerns."]
  },
  {
    id: "l-databases-indexes-transactions",
    stage: "computer-science",
    module: "Data Storage & Databases",
    title: "Databases, Indexes & Transactions",
    what: "A database stores structured data and provides mechanisms for querying, updating, indexing, concurrency control, and recovery. An index is an additional structure that makes certain queries faster at the cost of storage and write work.",
    why: "Applications eventually need durable shared data. Understanding why indexes exist and how transactions protect consistency prevents treating a database as a magical object that simply stores rows.",
    mentalModel: "A database table is a library collection. An index is the catalog that lets you find a book without scanning every shelf. A transaction is a controlled checkout process whose changes either complete according to its rules or are rolled back.",
    coreConcepts: ["relational tables and rows", "primary keys", "foreign keys", "queries", "indexes", "query selectivity", "transactions", "ACID", "atomicity and durability", "isolation and concurrent writes"],
    example: "-- Without an index, finding a user by email may require scanning many rows.\nCREATE INDEX idx_users_email ON users(email);\n\n-- The database can maintain the index so equality lookups can avoid a full scan in suitable cases.",
    code: "BEGIN;\n\nUPDATE accounts\nSET balance = balance - 500\nWHERE id = 1;\n\nUPDATE accounts\nSET balance = balance + 500\nWHERE id = 2;\n\nCOMMIT;\n\n-- In a real transfer, constraints and error handling must also be designed carefully.",
    commonMistakes: ["Adding indexes to every column without considering write and storage costs.", "Assuming an index guarantees a query will use it.", "Treating a transaction as only a way to group SQL statements without understanding isolation and failure behavior.", "Storing credentials or sensitive data without considering application-level security and database permissions."],
    practice: "Given tables for users, posts, and comments, identify primary keys, foreign keys, and two queries that could benefit from indexes.",
    challenge: "Design a money-transfer transaction and list the failure points that must not leave the system in a partially updated state.",
    miniProject: "Create a small relational database schema for a project tracker. Add realistic indexes, write common queries, and document why each index exists and which transaction boundaries matter.",
    resources: [{ title: "PostgreSQL Documentation — Transactions", url: "https://www.postgresql.org/docs/current/tutorial-transactions.html", note: "Practical official introduction to transactional behavior." }],
    mastery: ["I can explain why an index can speed reads while making writes more expensive.", "I can identify transaction boundaries for multi-step updates.", "I can explain ACID at a practical level rather than as four memorized words."]
  },
  {
    id: "l-security-fundamentals",
    stage: "computer-science",
    module: "Security Fundamentals",
    title: "Security Fundamentals & Threat Thinking",
    what: "Security is the practice of reducing the ways software and its data can be misused or compromised. Threat modeling starts by identifying assets, trust boundaries, attackers, attack surfaces, and the impact of failures.",
    why: "Security is not a final checklist. A developer who understands common failure modes can avoid entire classes of vulnerabilities before code reaches production.",
    mentalModel: "Treat an application like a building. Assets are what must be protected, entrances are attack surfaces, trust boundaries separate areas with different assumptions, authentication checks identity, and authorization decides what an identified user may do.",
    coreConcepts: ["assets and threats", "attack surface", "trust boundaries", "authentication vs authorization", "least privilege", "input validation", "injection", "XSS", "CSRF", "secrets management", "secure dependency hygiene"],
    example: "// Never build SQL by concatenating untrusted input.\n// BAD: `SELECT * FROM users WHERE email = '${email}'`\n// Prefer parameterized queries supplied by the database client/driver.",
    code: "// Browser output should treat user-provided text as data.\nconst message = userInput;\nelement.textContent = message;\n\n// Avoid assigning untrusted strings to innerHTML unless the content\n// has been intentionally sanitized for the exact context.",
    commonMistakes: ["Confusing authentication with authorization.", "Putting API keys, passwords, or private credentials in frontend source code.", "Trusting client-side validation as the only security control.", "Using innerHTML or dynamic SQL with untrusted input without context-appropriate defenses.", "Granting services more permissions than they need."],
    practice: "Threat-model a simple login application: list assets, users, trust boundaries, attack surfaces, and three realistic threats.",
    challenge: "Take a toy form that accepts a username and message. Identify how injection and XSS could arise, then redesign the data flow to keep input as data.",
    miniProject: "Perform a security review of one of your own small applications. Check secrets, authentication/authorization boundaries, input handling, output encoding, dependencies, and permissions. Produce a prioritized remediation list.",
    resources: [{ title: "OWASP Top 10", url: "https://owasp.org/www-project-top-ten/", note: "Industry reference for major web application security risks." }],
    mastery: ["I can distinguish authentication from authorization.", "I can identify common attack surfaces and trust boundaries.", "I can recognize unsafe handling of secrets, SQL input, and browser HTML.", "I treat security as a system property rather than only a frontend concern."]
  }
];
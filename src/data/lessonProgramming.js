export const lessonProgramming = [
  {
    id: "l-control-flow",
    stage: "programming",
    module: "Problem Solving with JavaScript",
    title: "Control Flow Deep Dive",
    what: "Control flow determines which instructions run, when they run, and how often they run. In JavaScript, conditions choose between paths while loops repeat work until a condition changes.",
    why: "Real programs are not straight-line scripts. They respond to different inputs, handle empty collections, retry work, and stop when a goal is reached. Reliable control flow is the foundation for all of those behaviors.",
    mentalModel: "Imagine a road network. An if statement is a junction, a loop is a circular road with an exit condition, and break is an emergency exit. Your job is to make every possible route intentional.",
    coreConcepts: ["if/else and else if", "truthy and falsy values", "switch for discrete cases", "for, while and do...while loops", "break and continue", "guard clauses and early returns", "avoiding accidental infinite loops"],
    example: "function classifyScore(score) {\n  if (score < 0 || score > 100) return \"invalid\";\n  if (score >= 90) return \"A\";\n  if (score >= 75) return \"B\";\n  if (score >= 60) return \"C\";\n  return \"Needs improvement\";\n}",
    code: "const tasks = [\"learn\", \"practice\", \"build\"];\n\nfor (const task of tasks) {\n  if (!task) continue;\n  console.log(`Working on: ${task}`);\n}",
    commonMistakes: ["Writing deeply nested if statements when a guard clause would make the path clearer.", "Forgetting to change the state used by a while-loop condition.", "Using switch for complex ranges where if/else communicates the rule better.", "Assuming every value behaves like a boolean without understanding truthiness."],
    practice: "Write a function that classifies a number as negative, zero, or positive and then extend it to reject non-numbers.",
    challenge: "Write a loop that searches an array for the first item matching a predicate and stops immediately when it finds one.",
    miniProject: "Build a console-based menu system that repeatedly accepts a command, performs an action, and exits only when the user chooses quit.",
    resources: [{ title: "MDN — Control flow and error handling", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Control_flow_and_error_handling", note: "Reference for JavaScript branching and loops." }],
    mastery: ["I can choose an appropriate branching or looping construct.", "I can trace every possible path through a small program.", "I can detect and prevent an infinite loop."]
  },
  {
    id: "l-strings-transformations",
    stage: "programming",
    module: "Problem Solving with JavaScript",
    title: "Strings & Data Transformation",
    what: "Strings represent text, but practical programming requires more than storing them: you must normalize, search, split, join, extract, validate, and transform text safely.",
    why: "User input, CSV-like data, URLs, filenames, logs, search queries, and API fields are all text. Strong string handling turns messy input into predictable data.",
    mentalModel: "Treat text processing as a pipeline: clean the input, break it into meaningful pieces, transform those pieces, then validate or combine the result.",
    coreConcepts: ["template literals", "length and indexing", "includes, startsWith and endsWith", "slice and substring", "split and join", "trim and case normalization", "replace and replaceAll", "regular expressions as a targeted tool"],
    example: "function normalizeEmail(input) {\n  return input.trim().toLowerCase();\n}\n\nconst tags = \"js, web, api\".split(\",\").map(tag => tag.trim());\nconsole.log(tags); // [\"js\", \"web\", \"api\"]",
    code: "function slugify(title) {\n  return title\n    .trim()\n    .toLowerCase()\n    .replace(/[^a-z0-9]+/g, \"-\")\n    .replace(/(^-|-$)/g, \"\");\n}\n\nconsole.log(slugify(\"  Learn JavaScript!  \")); // learn-javascript",
    commonMistakes: ["Mutating expectations: JavaScript string methods return new strings; strings are immutable.", "Forgetting to trim or normalize user input before comparing it.", "Using a complicated regular expression when simple string methods are clearer.", "Assuming text validation can be solved by checking only one superficial pattern."],
    practice: "Take a comma-separated string of skills and return a clean, lowercase array with empty entries removed.",
    challenge: "Write a function that masks the middle of an email address while preserving its domain.",
    miniProject: "Build a text-cleaning utility that accepts raw names, emails, tags, and search terms and outputs normalized values.",
    resources: [{ title: "MDN — String", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String", note: "Complete JavaScript String reference." }],
    mastery: ["I can select the right string method without trial-and-error chaining.", "I can build a clear transformation pipeline.", "I know when regular expressions are justified and when they add unnecessary complexity."]
  },
  {
    id: "l-errors-defensive",
    stage: "programming",
    module: "Reliable JavaScript",
    title: "Errors & Defensive Programming",
    what: "Errors are signals that an assumption failed. Defensive programming makes assumptions explicit, validates boundaries, and handles expected failures instead of allowing them to become mysterious bugs.",
    why: "Production inputs are incomplete, malformed, delayed, and sometimes wrong. Code that only works for the happy path is not reliable software.",
    mentalModel: "Treat every boundary like airport security: inspect what enters, reject invalid states early, and provide a useful failure path.",
    coreConcepts: ["syntax, runtime and logic errors", "throw and Error", "try/catch/finally", "validation at boundaries", "error propagation", "expected vs unexpected failures", "useful error messages"],
    example: "function parseAge(value) {\n  const age = Number(value);\n  if (!Number.isInteger(age) || age < 0) {\n    throw new Error(\"Age must be a non-negative integer\");\n  }\n  return age;\n}",
    code: "try {\n  const age = parseAge(userInput);\n  console.log(`Age: ${age}`);\n} catch (error) {\n  console.error(\"Invalid input:\", error.message);\n}",
    commonMistakes: ["Catching every error and silently ignoring it.", "Using exceptions for ordinary branching instead of validation and conditions.", "Logging an error without enough context to reproduce it.", "Validating data in one place and then assuming it remains valid forever."],
    practice: "Write a validator for a username and throw a descriptive Error when it violates your rules.",
    challenge: "Create a function that parses JSON and returns a safe result object such as { ok, value, error } without hiding the original failure.",
    miniProject: "Build a robust command-line calculator that rejects invalid numbers, unknown operators, division by zero, and malformed commands.",
    resources: [{ title: "MDN — Control flow and error handling", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Control_flow_and_error_handling", note: "Official guide to JavaScript errors and exception handling." }],
    mastery: ["I can distinguish syntax, runtime, and logic errors.", "I can validate external input before using it.", "I can handle expected failures without hiding unexpected ones."]
  },
  {
    id: "l-recursion-decomposition",
    stage: "programming",
    module: "Problem Solving with JavaScript",
    title: "Recursion & Problem Decomposition",
    what: "Recursion solves a problem by reducing it to smaller instances of the same problem. Every recursive solution needs a base case and a step that moves toward it.",
    why: "Recursion teaches a general engineering skill: turning a large problem into a smaller version that follows the same rule. It also prepares you for trees, graphs, parsers, and divide-and-conquer algorithms.",
    mentalModel: "A recursive function asks itself a smaller question. The calls form a stack of unfinished work; once the smallest case is solved, the answers return outward.",
    coreConcepts: ["base case", "recursive case", "progress toward termination", "call stack", "tracing recursive execution", "iterative alternatives", "divide-and-conquer thinking"],
    example: "function factorial(n) {\n  if (n <= 1) return 1;\n  return n * factorial(n - 1);\n}",
    code: "function countDown(n) {\n  if (n === 0) return;\n  console.log(n);\n  countDown(n - 1);\n}\n\ncountDown(3); // 3, 2, 1",
    commonMistakes: ["Missing the base case.", "Having a base case that is never reached.", "Changing the input in a direction that cannot reach the base case.", "Using recursion where a simple loop is clearer and safer."],
    practice: "Trace factorial(4) on paper and draw the call stack as calls are made and returned.",
    challenge: "Write a recursive function that sums all numbers in a nested array such as [1, [2, [3]], 4].",
    miniProject: "Build a recursive directory-tree walker using a nested JavaScript object and print each path with indentation.",
    resources: [{ title: "MDN — Recursion", url: "https://developer.mozilla.org/en-US/docs/Glossary/Recursion", note: "Concise reference definition and examples." }],
    mastery: ["I can identify the base and recursive cases.", "I can trace a recursive call stack by hand.", "I can explain when iteration is a better engineering choice."]
  },
  {
    id: "l-modules-organization",
    stage: "programming",
    module: "Code Organization",
    title: "Modules & Code Organization",
    what: "Modules let a program split related responsibilities into separate files with explicit imports and exports. Good module boundaries make dependencies visible and code easier to test and change.",
    why: "As programs grow, one file becomes a liability. Modules turn a large codebase into understandable units without relying on global variables.",
    mentalModel: "Think of modules as rooms with doors. Each room owns its implementation, and only the things deliberately placed at the door are available to other rooms.",
    coreConcepts: ["ES modules", "named exports", "default exports", "import paths", "module boundaries", "single responsibility", "dependency direction", "browser and Node.js module contexts"],
    example: "// math.js\nexport function add(a, b) {\n  return a + b;\n}\n\n// app.js\nimport { add } from \"./math.js\";\nconsole.log(add(2, 3));",
    code: "// user.js\nexport const formatUser = user => `${user.name} <${user.email}>`;\n\n// main.js\nimport { formatUser } from \"./user.js\";\n\nconst user = { name: \"Ari\", email: \"ari@example.com\" };\nconsole.log(formatUser(user));",
    commonMistakes: ["Using globals because imports feel slower to write.", "Creating modules that mix unrelated responsibilities.", "Confusing a default export with a named export.", "Using incorrect relative paths or forgetting the module environment."],
    practice: "Split a small calculator into separate math and formatting modules and import them into an entry file.",
    challenge: "Refactor a 100-line script into at least three modules while keeping the public API small.",
    miniProject: "Create a reusable JavaScript utility library with modules for strings, numbers, and validation plus a small demo entry point.",
    resources: [{ title: "MDN — JavaScript modules", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Modules", note: "Official ES module guide." }],
    mastery: ["I can create and consume named ES modules.", "I can explain why a dependency exists between two modules.", "I can split code by responsibility without creating unnecessary files."]
  },
  {
    id: "l-async-model",
    stage: "programming",
    module: "Asynchronous JavaScript",
    title: "Asynchronous JavaScript",
    what: "Asynchronous JavaScript lets a program start work that finishes later without blocking the main thread while it waits. The language uses the event loop and asynchronous APIs to coordinate that work.",
    why: "Browsers and servers constantly wait for files, networks, timers, databases, and user actions. Understanding async behavior prevents timing bugs and the false assumption that JavaScript simply runs one line until everything is finished.",
    mentalModel: "Think of a restaurant: the chef starts an order, then works on other orders instead of staring at the oven. When the order is ready, the kitchen receives the result and continues from there.",
    coreConcepts: ["synchronous vs asynchronous work", "call stack", "Web APIs/runtime", "task queue", "event loop", "callbacks", "timers", "why asynchronous completion changes control flow"],
    example: "console.log(\"A\");\nsetTimeout(() => console.log(\"B\"), 0);\nconsole.log(\"C\");\n// A, C, B — the timer callback waits for the current stack to finish.",
    code: "function delay(ms) {\n  return new Promise(resolve => setTimeout(resolve, ms));\n}\n\nconsole.log(\"start\");\ndelay(500).then(() => console.log(\"half a second later\"));\nconsole.log(\"continue\");",
    commonMistakes: ["Assuming setTimeout(fn, 0) runs immediately.", "Reading asynchronous code as if the callback result were available synchronously.", "Creating callback nesting instead of using a clearer Promise-based flow.", "Ignoring rejected asynchronous operations."],
    practice: "Predict the output order of three console.log statements mixed with two setTimeout callbacks before running the code.",
    challenge: "Create a callback-based function that simulates loading data after a delay and then identify where callback nesting becomes hard to maintain.",
    miniProject: "Build a simulated dashboard loader that asynchronously loads three independent data sets and reports when each becomes available.",
    resources: [{ title: "MDN — Asynchronous JavaScript", url: "https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Asynchronous", note: "Structured introduction to async JavaScript." }],
    mastery: ["I can explain why asynchronous callbacks run later.", "I can trace a simple event-loop example.", "I can identify code that incorrectly assumes an async result is already available."]
  },
  {
    id: "l-promises",
    stage: "programming",
    module: "Asynchronous JavaScript",
    title: "Promises & Async Control Flow",
    what: "A Promise represents the eventual outcome of an asynchronous operation: pending, fulfilled, or rejected. Promise methods let you compose asynchronous steps without deeply nested callbacks.",
    why: "Promises are the standard abstraction behind modern JavaScript APIs. Understanding them makes async/await understandable rather than magical.",
    mentalModel: "A Promise is a claim ticket for future work. You do not have the result yet; you have a controlled way to say what should happen when the result arrives or fails.",
    coreConcepts: ["pending, fulfilled and rejected", "then", "catch", "finally", "Promise.resolve and Promise.reject", "Promise.all", "Promise.allSettled", "chaining and returned promises"],
    example: "fetchUser()\n  .then(user => fetchPosts(user.id))\n  .then(posts => console.log(posts))\n  .catch(error => console.error(error))\n  .finally(() => console.log(\"finished\"));",
    code: "const first = Promise.resolve(10);\n\nfirst\n  .then(value => value * 2)\n  .then(value => console.log(value))\n  .catch(error => console.error(error));",
    commonMistakes: ["Forgetting to return a Promise from a then callback when another async step depends on it.", "Adding multiple unrelated handlers instead of composing one clear chain.", "Assuming Promise.all continues successfully when one input rejects.", "Handling errors only at the final UI layer when a lower layer can add useful context."],
    practice: "Create three resolved Promises and use Promise.all to calculate a result from all three values.",
    challenge: "Compare Promise.all and Promise.allSettled with one successful and one rejected Promise and explain the different outcomes.",
    miniProject: "Build a Promise-based fake API layer with functions for users, posts, and comments, then compose them into one loading flow.",
    resources: [{ title: "MDN — Promise", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise", note: "Complete Promise reference." }],
    mastery: ["I can explain the three Promise states.", "I can chain dependent asynchronous operations.", "I can choose between Promise.all and Promise.allSettled based on failure requirements."]
  },
  {
    id: "l-async-await",
    stage: "programming",
    module: "Asynchronous JavaScript",
    title: "async/await & Reliable Async Code",
    what: "async/await provides readable syntax for Promise-based asynchronous code. An async function returns a Promise, and await pauses that function until the awaited Promise settles without blocking the JavaScript runtime.",
    why: "Most application code becomes easier to read when asynchronous steps look like the sequence they represent. The important part is retaining Promise semantics and explicit error handling.",
    mentalModel: "await is a checkpoint inside one async function: 'resume this function when this Promise finishes.' It does not freeze the browser or server for everyone else.",
    coreConcepts: ["async functions", "await", "try/catch with async code", "sequential vs parallel awaits", "Promise.all for concurrency", "returning values from async functions", "propagating failures"],
    example: "async function loadUser(id) {\n  const response = await fetch(`/api/users/${id}`);\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json();\n}",
    code: "async function loadDashboard() {\n  try {\n    const [user, stats] = await Promise.all([\n      loadUser(42),\n      loadStats()\n    ]);\n    return { user, stats };\n  } catch (error) {\n    console.error(\"Dashboard failed:\", error);\n    throw error;\n  }\n}",
    commonMistakes: ["Awaiting independent operations one after another and unnecessarily increasing total time.", "Forgetting that an async function always returns a Promise.", "Using try/catch without deciding what the caller should do after the error.", "Assuming await makes CPU-heavy synchronous work non-blocking."],
    practice: "Rewrite a simple Promise chain using async/await and preserve equivalent error behavior.",
    challenge: "Write a function that loads two independent resources in parallel but fails with a useful combined message if either request fails.",
    miniProject: "Create an async command-line or browser-console data loader that fetches multiple resources, displays loading states, and handles failure cleanly.",
    resources: [{ title: "MDN — async function", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function", note: "Official async function reference." }],
    mastery: ["I can explain what async and await do to Promise-based control flow.", "I can distinguish sequential work from independent parallel work.", "I can design an explicit failure path for async operations."]
  },
  {
    id: "l-fetch-apis",
    stage: "programming",
    module: "Working With Data",
    title: "Working With APIs",
    what: "An API is a contract through which software exposes data or operations. In JavaScript, fetch provides a standard way to make HTTP requests and process responses asynchronously.",
    why: "Modern applications rarely contain all their data locally. They communicate with APIs for authentication, products, weather, payments, analytics, and almost every external service.",
    mentalModel: "An API is a service counter with a defined order format. You send a request containing the required information, receive a response with a status and payload, and decide whether that response is usable.",
    coreConcepts: ["HTTP methods", "request URL and query parameters", "headers", "JSON serialization", "Response status and ok", "response.json()", "GET vs POST", "network failure vs HTTP failure", "basic API contracts"],
    example: "async function getUsers() {\n  const response = await fetch(\"https://example.com/api/users\");\n  if (!response.ok) throw new Error(`Request failed: ${response.status}`);\n  return response.json();\n}",
    code: "async function createUser(user) {\n  const response = await fetch(\"/api/users\", {\n    method: \"POST\",\n    headers: { \"Content-Type\": \"application/json\" },\n    body: JSON.stringify(user)\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json();\n}",
    commonMistakes: ["Treating every HTTP response as successful because fetch itself resolved.", "Forgetting to serialize a JSON request body.", "Hard-coding secrets such as API keys into browser code.", "Assuming the response shape without inspecting or documenting the API contract."],
    practice: "Use a public test API to make one GET request, inspect the status, parse JSON, and print three selected fields.",
    challenge: "Add query parameters and handle three categories separately: network failure, non-2xx HTTP response, and invalid JSON/data.",
    miniProject: "Build a console-based API explorer that searches a public API, prints selected fields, and reports useful errors.",
    resources: [{ title: "MDN — Fetch API", url: "https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API", note: "Official Fetch API documentation." }],
    mastery: ["I can make and inspect a basic HTTP request from JavaScript.", "I can distinguish transport failure from an HTTP error status.", "I can safely parse and validate API data before using it."]
  },
  {
    id: "l-state-dataflow",
    stage: "programming",
    module: "Program State & Architecture",
    title: "Programming With State",
    what: "State is the information that determines what a program should do or display at a given moment. Good programs make state transitions explicit instead of scattering mutations across unrelated code.",
    why: "Interactive software is mostly state changing over time: a task is added, a request becomes loading, a user logs in, a form becomes invalid. Understanding state now makes later frontend and backend architecture much easier.",
    mentalModel: "Think of a state machine: the program occupies one known state, an event occurs, and a defined transition produces the next state.",
    coreConcepts: ["state vs derived data", "events and transitions", "single source of truth", "immutability as a useful discipline", "state machines", "separation of data and presentation", "avoiding duplicated state"],
    example: "const state = { status: \"idle\", count: 0 };\n\nfunction start(state) {\n  return { ...state, status: \"loading\" };\n}\n\nconst next = start(state);\nconsole.log(state.status); // idle\nconsole.log(next.status);  // loading",
    code: "function reducer(state, event) {\n  switch (event.type) {\n    case \"increment\":\n      return { ...state, count: state.count + 1 };\n    case \"reset\":\n      return { ...state, count: 0 };\n    default:\n      return state;\n  }\n}",
    commonMistakes: ["Storing values that can be calculated from existing state.", "Allowing multiple unrelated pieces of code to mutate the same object.", "Creating impossible combinations such as loading=true and error=true without defining what they mean.", "Mixing UI rendering decisions into low-level data manipulation."],
    practice: "Model a login flow with explicit states such as idle, loading, authenticated, and error.",
    challenge: "Write a small reducer for a shopping cart with add, remove, and clear events without mutating the original state.",
    miniProject: "Build a state-driven console to-do application where every user action is represented as an event and produces a predictable next state.",
    resources: [{ title: "MDN — JavaScript objects and state concepts", url: "https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Objects", note: "Useful reference for modeling data with objects." }],
    mastery: ["I can distinguish source state from derived values.", "I can model a small state machine and its transitions.", "I can update state predictably without accidental shared mutation."]
  },
  {
    id: "l-oop-practical",
    stage: "programming",
    module: "Object-Oriented JavaScript",
    title: "Object-Oriented Programming — Practical",
    what: "Object-oriented programming organizes behavior and data around objects. JavaScript supports classes and prototypes, but good JavaScript does not require every problem to become a class hierarchy.",
    why: "You will encounter classes in browsers, libraries, Node.js projects, and existing codebases. The goal is to understand and use them when they clarify ownership and behavior, not to force OOP everywhere.",
    mentalModel: "A class is a blueprint for objects with shared behavior. An instance is one concrete object created from that blueprint. In JavaScript, classes are syntax built on the prototype system.",
    coreConcepts: ["classes and instances", "constructor", "methods", "private fields", "inheritance", "composition", "prototype chain", "when functions and plain objects are simpler"],
    example: "class BankAccount {\n  #balance = 0;\n\n  constructor(owner) {\n    this.owner = owner;\n  }\n\n  deposit(amount) {\n    if (amount <= 0) throw new Error(\"Amount must be positive\");\n    this.#balance += amount;\n  }\n\n  getBalance() {\n    return this.#balance;\n  }\n}",
    code: "class Timer {\n  constructor(label) {\n    this.label = label;\n  }\n\n  describe() {\n    return `Timer: ${this.label}`;\n  }\n}\n\nconst timer = new Timer(\"Focus\");\nconsole.log(timer.describe());",
    commonMistakes: ["Creating classes solely because the code has multiple objects.", "Using inheritance where composition would produce simpler dependencies.", "Putting too much mutable state inside an object.", "Forgetting that class methods use this based on how the method is called."],
    practice: "Create a Book class with title, author, and a method that returns a formatted description.",
    challenge: "Model a notification system with composition: a Notification object delegates delivery to a separate channel implementation.",
    miniProject: "Build a small library-management domain model using classes only where they provide clear ownership, then document why each class exists.",
    resources: [{ title: "MDN — Classes", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes", note: "Official JavaScript class reference." }],
    mastery: ["I can create and use a class confidently.", "I understand the relationship between classes and prototypes at a practical level.", "I can justify composition, classes, or plain functions based on the problem rather than habit."]
  }
];